import { View, Text } from 'react-native';

export const Home = () => {
	return (
		<View style={{ marginTop: 200 }}>
			<Text style={{ color: 'white' }}>Sou o modals index</Text>
		</View>
	);
};

export default Home;
